# FineTune Lab Backend Configuration
# BACKEND_URL: Platform URL (use https://app.finetunelab.ai for production)
# For local development, use: http://localhost:3000
BACKEND_URL=https://app.finetunelab.ai

# API_KEY: Worker API key (REQUIRED)
# Get this from: Platform UI → Worker Agents → Setup & Download → Generate API Key
# Must start with "wak_" and have worker scope
# IMPORTANT: Replace this placeholder with your actual API key
API_KEY=wak_your_worker_api_key_here

# Worker Identification (Optional - auto-detected if not set)
# WORKER_HOSTNAME: Custom hostname (default: auto-detect from system)
# WORKER_PLATFORM: Platform override (linux/darwin/windows, default: auto-detect)
# WORKER_VERSION: Worker version (default: 0.1.0)
# WORKER_CAPABILITIES: Comma-separated list (default: ["training"])
# Uncomment to override:
# WORKER_HOSTNAME=my-custom-hostname
# WORKER_PLATFORM=linux
# WORKER_VERSION=0.1.0

# Agent Server
HOST=0.0.0.0
PORT=8000

# Paths
MODELS_DIR=./models
DATASETS_DIR=./datasets
CHECKPOINTS_DIR=./checkpoints
LOGS_DIR=./logs

# GPU Settings
GPU_MEMORY_FRACTION=0.9
ENABLE_MIXED_PRECISION=true

# Training Settings
MAX_CONCURRENT_JOBS=1
CHECKPOINT_INTERVAL_STEPS=500
METRICS_REPORT_INTERVAL_STEPS=10

# Monitoring
GPU_MONITORING_INTERVAL_SECONDS=5
HEARTBEAT_INTERVAL_SECONDS=30

# Logging
LOG_LEVEL=INFO
